This project tries to show issues related Robot Framework resource loading/reloading.

Project can be installed with command
```
poetry install
```

And then executed with
```
poetry run robot -A config/config.txt testsuite
```